<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Welcome to URL Shortener</h1>
    </header>
    <main>
        <div class="card">
            <form method="post" action="/url">
                <input type="url" name="url" placeholder="Enter your URL here (e.g., http://www.example.com/)" required/>
                <button type="submit">Shorten URL</button>
            </form>
        </div>
        
        <% if (locals.shortUrl) { %>
            <div class="result-card">
                <p>Your Generated Short URL:</p>
                <a href="<%= shortUrl %>"><%= shortUrl %></a>
            </div>
        <% } %>
    </main>
    <footer>
        <p>&copy; 2025 URL Shortener</p>
    </footer>
</body>
</html>